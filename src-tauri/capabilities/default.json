{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window: core IPC and events only. The webview never touches the network or the filesystem; everything goes through the shell's commands.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
